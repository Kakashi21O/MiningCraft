# MiningCraft configuration.
# Loaded and validated by Pydantic models at startup (see src/miningcraft/core/config.py).
# Never store secrets in this file; use environment variables instead.

server:
  host: "Atharva200509.aternos.me"
  port: 11295
  version: "1.20.1"

bot:
  username: "Nova"

engine:
  tick_rate: 20

mining:
  tunnel_length: 64
  branch_spacing: 3
  speed: 1.0

safety:
  lava_distance: 3
  water_distance: 2

logging:
  level: "INFO"
  format: "json"  # "json" in production, "pretty" in development
